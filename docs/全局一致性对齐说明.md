# 全局一致性对齐说明

> 更新时间：2026-04-18  
> 对齐原则：**以当前代码实现为唯一事实来源**（Code as Source of Truth）

---

## 0. 品牌与命名口径（统一）

- **中文正式名**：**福帮手数据智能化系统**（与根目录 `README.md`、主标题一致）。  
- **中文简称 / 对外口号**：**福帮手 FBSir**（与「福帮手FBSir，幸福有AI，幸运有你」品牌句一致；文档中可写作 **福帮手 FBSir** 以便阅读）。  
- **技术标识**：**WxFbsir**（仓库、包名、配置前缀 `wxfbsir`）。  
- **勿再使用**：**「微信福帮手」** 作为产品名（易与「微信公众平台」混淆；微信相关能力在文档中单独表述为微信公众号 / 企业微信 / 微信扫码等）。

---

## 1. 架构与模块口径（统一）

### 1.1 主工程（根目录聚合）
- 根 `pom.xml` 为 Maven 聚合工程（`packaging=pom`）
- 聚合模块：
  - `WxFbsir-admin`
  - `WxFbsir-framework`
  - `WxFbsir-system`
  - `WxFbsir-quartz`
  - `WxFbsir-generator`
  - `WxFbsir-common`
  - `WxFbsir-business`

### 1.2 独立工程
- `WxFbsir-engine` 为**独立部署**工程，不在根聚合模块内。此为**主节点（Admin 主干聚合）/ 副节点（Engine）**架构下的**既定设计**：副节点独立版本线、独立打包与发布，**不**纳入根 `pom` 的 Maven reactor；请勿将「未聚合」理解为缺陷或待合并项。
- `WxFbsir-ui` 为前端工程（Vue 3 + Vite）

### 1.3 启动入口
- Admin 启动类：`WxFbsir-admin/src/main/java/com/wx/fbsir/WxFbsirApplication.java`
- Engine 启动类：`WxFbsir-engine/src/main/java/com/wx/fbsir/engine/WxFbsirEngineApplication.java`
- 前端入口：`WxFbsir-ui/src/main.js`

### 1.4 主副节点 Maven 边界（全局一致性审计清单 · 禁止误改）

> **用途**：评审 PR、写 CI、写文档时，先对照本节，避免把「架构分轨」当成「缺陷」去改。

| 类别 | 要求 |
|------|------|
| **根 `pom.xml`** | `<modules>` **仅**包含 Admin / framework / system / quartz / generator / common / business；**不得**新增 `<module>WxFbsir-engine</module>`。 |
| **Engine 构建** | 在 **`WxFbsir-engine/`** 目录执行 `mvn test`、`mvn package`；与根目录 `mvn test` **分轨**，两条命令覆盖不同工程。 |
| **门禁脚本** | `tools/run-ci-aigc-checks.ps1` 仅覆盖根聚合内模块 + 前端；**不**隐含「已测 Engine」；若需 Engine 门禁，须在 `WxFbsir-engine` 下**另起一步**。 |
| **文档表述** | 禁止将「Engine 未纳入根 reactor」写成待办、缺口、技术债；应表述为**主节点 / 副节点**刻意划分（见 §1.2、`docs/项目白皮书.md` §6.2）。 |
| **易误判** | 「一次 `mvn test` 测全仓 Java」在现结构下**不成立**；全量需 **根 `mvn test` + `WxFbsir-engine` 目录 `mvn test`**（顺序可自定）。 |

**变更前自检（Reviewer 可复制）**

- [ ] 未修改根 `pom.xml` 的 `<modules>` 以纳入 `WxFbsir-engine`
- [ ] 新增/修改的 CI 或文档未暗示「根 reactor 已包含 Engine」
- [ ] 涉及 Engine 的版本、打包说明仍指向 `WxFbsir-engine/pom.xml` 与 `engine.version`

---

## 2. 版本口径（统一）

- **主干工程版本**：`1.0.0`
  - 来源：根 `pom.xml`、`WxFbsir-ui/package.json`、Admin 配置
- **Engine 节点版本**：`1.3.1`
  - 来源：`WxFbsir-engine/pom.xml` 的 `engine.version`

### 文档书写规范
- 禁止在部署文档硬编码历史版本号（如 `1.2.6` / `1.6.5`）
- Engine JAR 统一写法：`wxfbsir-engine-[engine.version].jar`

### 2.2 源码托管与克隆口径（统一）

- **主仓库（推荐）**：[https://github.com/duhongchao-Fbsir/FBsir](https://github.com/duhongchao-Fbsir/FBsir)
- **默认分支**：`fbsir`
- **克隆后目录名**：一般为 `FBsir`（与 Maven 模块前缀 `WxFbsir-*` 并存，勿混写为历史路径 `U3W-AI`）
- **LICENSE 链接**：以 GitHub 上 `fbsir` 分支的 `LICENSE` 为准（见根目录 `README.md` 徽章）

### 2.3 AI 助手关联域（扩展）

- **全局洞察与治理清单**（能力对齐、产出物、日志、OpenAPI、复用规范）：见 [AI助手关联域全局洞察与一致性升级.md](架构与规范/AI助手关联域全局洞察与一致性升级.md)。

---

## 3. 配置键与路径口径（统一）

### 3.1 Engine 重连配置键
以 `WxFbsir-engine/src/main/resources/application.yml` 为准：
- `max-retries`
- `initial-delay`
- `max-delay`
- `backoff-multiplier`

### 3.2 Admin 配置文件路径
统一使用：
- `WxFbsir-admin/src/main/resources/application.yml`
- `WxFbsir-admin/src/main/resources/application-druid.yml`

### 3.2.1 JDBC 与中文编码（必查）
- **JDBC URL** 中 `characterEncoding` 必须为 **`UTF-8`**（大写），勿使用小写 `utf8`，以免与 MySQL 历史 `utf8`(utf8mb3) 混淆导致菜单/业务中文变成 `???`。
- **Druid**：在 `DruidProperties` 中对连接执行 `SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci`，与会话、表 `utf8mb4_unicode_ci` 一致。
- **Spring**：`spring.servlet.encoding` 启用 UTF-8 并 `force: true`，保证请求/响应层一致。
- **已乱码数据**：执行 `sql/update_20260414_修复菜单与白名单中文乱码.sql`（或备份后按需 UPDATE），客户端使用 `--default-character-set=utf8mb4`。

### 3.3 文档链接路径
Engine 相关主文档统一位于：
- `docs/功能说明/engine/Playwright框架完整指南.md`
- `docs/功能说明/engine/WebSocket通信完整指南.md`

### 3.4 HTTP 纳管主机（OpenClaw / Hermes）口径
- **数据表**：`ws_host_whitelist`，字段 `host_type` 为 `openclaw` 或 `hermes` 且已配置 `health_check_url` 时，参与定时与手动 HTTP 健康检查。
- **Mapper**：`WsHostWhitelistMapper#selectHostsForHttpHealthCheck()`（**非**历史方法名 `selectOpenclawHosts`）。
- **定时任务类名**：`OpenClawHealthChecker`（类名沿用，实现已覆盖 OpenClaw 与 Hermes）。
- **周期与规则**：约 30s；GET；连接/读取超时 5s；HTTP **2xx** → `online_status=online`。
- **后端接口**：`HostWhitelistController`，前缀 `/business/host/whitelist`；导出为 **`POST /export`**（与前端 `proxy.download` / `whitelist.js` 一致）。
- **自动化测试**：`WxFbsir-business/src/test/java/.../OpenClawHealthCheckerTest`、`WsHostWhitelistMapperIT`、`HostWhitelistControllerStandaloneTest`。

---

## 4. 本次已完成的对齐项

1. 品牌口径统一：产品正式名 **福帮手数据智能化系统**，简称 **福帮手 FBSir**，技术名 **WxFbsir**；全库去除 **「微信福帮手」** 作为产品称谓（见上文 §0）。
2. 修正 `WxFbsir-engine/README.md` 中主副节点术语、配置示例、启动命令、文档链接与启动类命名。
3. 修正 `部署文档.md` 中 Engine 固定旧版本号与跳转链接。
4. 修正 `docs/服务器部署/完整服务器部署文档.md` 中 reconnect 键名与 Engine JAR 版本写法。
5. 修正 `docs/功能说明/engine/engine功能/AIGC框架完整功能说明.md` 中 Admin 启动模块与默认访问地址。
6. 修正 `docs/功能说明/business功能/面试助手功能说明.md` 中 `main/resources` 错误路径。
7. 修正 `WxFbsir-ui/src/views/business/debug/README.md` 中 WebSocket 指南错链。
8. 修正 `项目结构说明.md` 中不存在的 `WxChat-demo` 目录描述及 Engine 依赖表述。
9. 更新 `README.md`、`docs/README.md` 并增加本说明文档入口。
10. Hermes 主机纳管与 `selectHostsForHttpHealthCheck`、文档与 API 对齐；补全主机白名单 **导出** 后端接口与 `WsHostWhitelist` 导出列注解。
11. 全库文档与 `package.json` 仓库地址对齐 **GitHub 主仓库**；废弃文档中的 `U3W-AI` 根目录、`gitee.com/U3W-AI/U3W-AI` 克隆示例。**AIGC 浏览器对话**中的「Gitee AI Chat / 秘塔」已从 Engine 下架；**Gitee OAuth / 用户画像 / 评测**（`business/gitee`）仍为独立业务，与源码托管使用的 **gitee.com** 无混同。

---

## 5. 后续文档维护约定

- 新增/修改功能时，必须同时更新对应功能文档与本对齐说明。
- 文档中的启动命令、路径、端口、配置键必须可在代码中定位验证。
- 版本信息按“主干版本 + Engine 独立版本”双口径维护，不再混用单一版本号。
- **主副节点 Maven**：任何文档或 CI 若涉及「全量 Java 构建/测试」，须与 **§1.4** 一致，不得暗示或实现「将 `WxFbsir-engine` 并入根 `<modules>`」。
- **`docs/项目白皮书.md`**：面向多类读者的全维度总览；细节以专题文档与代码为准，避免与白皮书重复堆砌实现细节。
