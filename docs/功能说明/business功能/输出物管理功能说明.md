# AIGC 输出物管理功能说明

AIGC 输出物管理功能，用于将 AI 会话中的探讨结果沉淀为结构化内容，并支持编辑、导出及推送等能力，提升结果的可复用性与业务价值。

------

##  目录

- 功能概述
- 使用场景
- 配置说明
- 接口说明
- 权限控制
- 核心特性
- 数据库设计
- 代码位置
- 使用示例
- 字段说明

------

## 一、功能概述

本功能基于 AIGC 会话能力扩展，实现“输出物”管理闭环，主要包括：

1. **输出物生成**：基于会话内容自动生成结构化摘要
2. **输出物编辑**：支持用户对生成内容进行修改
3. **输出物保存**：支持持久化保存至会话数据
4. **导出能力**：支持 Markdown 与 JSON 导出
5. **Webhook 推送**：支持推送至企业微信等外部系统
6. **权限控制**：基于菜单权限控制功能访问
7. **异常处理**：完善的边界与异常处理机制
8. **日志记录**：详细的操作日志记录

------

## 二、使用场景

- AI 探讨结果沉淀为结构化内容
- 输出物编辑与二次加工
- 导出文档用于汇报或记录
- 推送至企业微信、自动化系统等外部工具
- 提升 AI 输出结果的复用能力

------

## 三、配置说明

### **1.数据库的填充**

在 `data` 字段中的data中新增outputArtifacts

```
{
  "data": 
  {
    "answer": "AI回复内容",
    "outputArtifacts": 
    {
        "id": "art-xxx",
        "type": "decision_summary",
        "title": "本期结论",
        "content": "问题：xxx\n结论摘要：xxx",
        "createdAt": "时间"
    }
  }
 }
```



------

## 四、接口说明

### 后端接口

| 接口地址                                  | 请求方式 | 权限     | 说明         |
| ----------------------------------------- | -------- | -------- | ------------ |
| `/aigc/output/generate`                   | POST     | 登录用户 | 生成输出物   |
| `/aigc/output/save`                       | POST     | 登录用户 | 保存输出物   |
| `/aigc/output/exportMarkdown/{sessionId}` | GET      | 登录用户 | 导出Markdown |
| `/aigc/output/exportJson/{sessionId}`     | GET      | 登录用户 | 导出JSON     |
| `/aigc/output/pushWebhook`                | POST     | 登录用户 | Webhook推送  |

------

### 前端接口

位置：

```
WxFbsir-ui/src/api/business/content/aigc/output.js
```

主要方法：

```
generateOutputArtifact()
saveOutputArtifact()
exportOutputMarkdown()
exportOutputJson()
pushOutputWebhook()
```

------

## 五、权限控制

### 权限配置

基于 `sys_menu` 表新增以下权限：

| 菜单ID | 菜单名称     | 类型    | 权限标识                       |
| ------ | ------------ | ------- | ------------------------------ |
| 1526   | 生成输出物   | 按钮(F) | business:output:generate       |
| 1527   | 导出Markdown | 按钮(F) | business:output:exportMarkdown |
| 1529   | 推送Webhook  | 按钮(F) | business:output:pushWebhook    |
| 1530   | 保存输出物   | 按钮(F) | business:output:save           |

### 权限说明

- 采用按钮级权限控制
- 未授权用户不可执行相关操作
- 需执行 SQL 脚本后生效

------

## 六、核心特性

### 1. 结构化输出

将 AI 会话结果转为统一结构：

生成规则：

```
问题：userPrompt
结论摘要：data.answer
```

示例：

```
问题：如何部署AI服务
结论摘要：建议使用Docker部署并通过Nginx进行反向代理
```

权限配置SQL

```
-- 生成输出物

insert into sys_menu values('1526', '生成输出物', '153', '1', '', '', '', '', 1, 0, 'F', '0', '0', 'business:output:generate', '#', 'admin', sysdate(), '', null, '生成输出物权限');

-- 导出Markdown

insert into sys_menu values('1527', '导出Markdown', '153', '2', '', '', '', '', 1, 0, 'F', '0', '0', 'business:output:exportMarkdown', '#', 'admin', sysdate(), '', null, '导出Markdown权限');

-- 导出JSON

insert into sys_menu values('1528', '导出JSON', '153', '3', '', '', '', '', 1, 0, 'F', '0', '0', 'business:output:exportJson', '#', 'admin', sysdate(), '', null, '导出JSON权限');

-- 推送Webhook

insert into sys_menu values('1529', '推送Webhook', '153', '4', '', '', '', '', 1, 0, 'F', '0', '0', 'business:output:pushWebhook', '#', 'admin', sysdate(), '', null, '推送Webhook权限');

-- 保存输出物

insert into sys_menu values('1530', '保存输出物', '153', '5', '', '', '', '', 1, 0, 'F', '0', '0', 'business:output:save', '#', 'admin', sysdate(), '', null, '保存输出物权限');

```

------

### 2. 可编辑能力

- 支持修改标题与内容
- 保存后覆盖原输出物

------

### 3. 统一返回格式

- 成功返回

```
return AjaxResult.success(result);
return AjaxResult.success("保存成功", result);
```

- 失败返回

```
return AjaxResult.error("生成输出物失败: " + e.getMessage());
return AjaxResult.error(String.valueOf(result.get("message")));
return AjaxResult.error("导出JSON失败: " + e.getMessage());
return AjaxResult.error("推送Webhook失败: " + e.getMessage());
return AjaxResult.error("sessionId不能为空");
return AjaxResult.error("artifactId不能为空");
return AjaxResult.error("输出物内容不能为空");
return AjaxResult.error("保存输出物失败: " + e.getMessage());
```

------

### 4. Webhook 推送

- 支持 HTTP 推送
- 当前已验证企业微信机器人
- 支持 JSON / Markdown 两种格式

------

### 5. 异常处理

覆盖以下场景：

- 会话不存在
- 会话归属错误
- AI结果为空
- 输出物不存在
- 导出失败
- Webhook地址非法
- Webhook推送失败

统一的异常处理机制，确保错误信息清晰：

例如：

```
try {
    // 业务逻辑
} catch (Exception e) {
    logger.error("[输出物推送] 接口异常 - sessionId: {}, format: {}, webhookUrl: {}, 错误类型: {}, 错		误信息: {}",params.get("sessionId"),
    params.get("format"),params.get("webhookUrl"),
    e.getClass().getSimpleName(), e.getMessage());
    return AjaxResult.error("操作失败: " + e.getMessage());
}
```

------

### 6.日志记录

完善的日志记录，便于问题排查和操作追踪：

例如：

```
log.info("[输出物推送] 推送成功 - sessionId: {}, webhookUrl: {}, statusCode: {}",
        sessionId, webhookUrl, statusCode);
        
log.info("[输出物保存] 保存成功 - sessionId: {}, artifactId: {}",
                sessionId, updatedArtifact.get("id"));
```

------

## 七、代码位置

### 后端

```
WxFbsir-business/src/main/java/com/wx/fbsir/business/aigc/
├── controller/AigcController.java
├── service/IAigcService.java
├── service/impl/AigcServiceImpl.java
```

------

### 前端

```
WxFbsir-ui/src/
├── api/business/content/aigc/output.js
└── views/business/content/aigc/index.vue
```

------

## 八、使用示例

### 1. 生成输出物

```
点击「生成输出物」按钮
```

------

### 2. 编辑并保存

```
修改标题/内容 → 点击保存
```

------

### 3. 导出

```
点击导出Markdown
```

------

### 4. Webhook 推送

```
输入合法Webhook地址 → 点击推送
```

------

## 九、字段说明

| 字段名    | 类型   | 必填 | 说明                    |
| --------- | ------ | ---- | ----------------------- |
| id        | String | ✅    | 输出物唯一标识          |
| type      | String | ✅    | 输出物类型              |
| title     | String | ✅    | 输出物标题              |
| content   | String | ✅    | 输出物内容              |
| createdAt | String | ✅    | 创建时间（ISO时间格式） |

------

## 十、部署与验证

### 部署环境

- 本地开发环境
- Linux服务器环境

### 验证结果

- 已完成本地功能测试
- 已在 Linux 环境部署并通过域名访问验证
- 输出物生成、编辑、保存、导出及推送功能正常
- 企业微信 Webhook 推送验证通过

------

## 十一、注意事项

1. 输出物基于会话生成，需确保会话存在
2. Webhook 地址需合法
3. 导出前需存在输出物内容
4. SQL 权限需提前执行
5. 多次生成输出物会覆盖旧数据
6. 多 AI 同时产出时的合并/优先级以产品与 `mergeAiResults` 实现为准；勿再以已下架的 Gitee AI Chat 场景为默认示例。