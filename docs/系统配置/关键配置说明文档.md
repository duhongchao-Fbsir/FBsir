# 福帮手（WxFbsir）关键配置说明文档

## 项目概述

---

## 📋 目录
- [项目概述](#项目概述)
- [一、项目结构](#一项目结构)
- [二、核心配置文件](#二核心配置文件)
  - [2.1 主节点配置文件](#21-主节点配置文件-wxfbsir-adminsrcmainresourcesapplicationyml)
  - [2.2 前端配置文件](#22-前端配置文件-wxfbsir-uiviteconfigjs)
  - [2.3 引擎节点配置文件](#23-引擎节点配置文件-wxfbsir-enginesrcmainresourcesapplicationyml)
  - [2.4 项目根配置](#24-项目根配置-pomxml)
- [三、环境变量配置](#三环境变量配置)
  - [3.1 系统环境变量](#31-系统环境变量)
  - [3.2 项目环境变量](#32-项目环境变量)
- [四、数据库配置](#四数据库配置)
  - [4.1 数据库脚本](#41-数据库脚本)
  - [4.2 数据库配置要点](#42-数据库配置要点)
- [五、安全配置](#五安全配置)
  - [5.1 令牌配置](#51-令牌配置)
  - [5.2 XSS防护配置](#52-xss防护配置)
  - [5.3 防盗链配置](#53-防盗链配置)
- [六、缓存配置](#六缓存配置)
  - [6.1 Redis缓存配置](#61-redis缓存配置)
  - [6.2 本地缓存配置](#62-本地缓存配置)
- [七、日志配置](#七日志配置)
  - [7.1 日志级别配置](#71-日志级别配置)
  - [7.2 日志文件配置](#72-日志文件配置)
- [八、定时任务配置](#八定时任务配置)
  - [8.1 Quartz配置](#81-quartz配置)
  - [8.2 自定义定时任务](#82-自定义定时任务)
- [九、文件上传配置](#九文件上传配置)
  - [9.1 上传限制](#91-上传限制)
  - [9.2 存储路径](#92-存储路径)
- [十、部署配置](#十部署配置)
  - [10.1 Admin服务部署](#101-admin服务部署)
  - [10.2 Engine服务部署](#102-engine服务部署)
  - [10.3 前端部署](#103-前端部署)
- [十一、性能优化配置](#十一性能优化配置)
  - [11.1 连接池配置](#111-连接池配置)
  - [11.2 缓存优化](#112-缓存优化)
- [十二、监控配置](#十二监控配置)
  - [12.1 健康检查](#121-健康检查)
  - [12.2 指标监控](#122-指标监控)
- [十三、开发环境配置](#十三开发环境配置)
  - [13.1 IDE配置](#131-ide配置)
  - [13.2 开发工具](#132-开发工具)
- [十四、配置注意事项](#十四配置注意事项)

---

## 一、项目结构

```
WxFbsir/
├── WxFbsir-admin/          # 后台管理服务（主节点）
├── WxFbsir-business/       # 业务模块
├── WxFbsir-common/         # 通用工具模块
├── WxFbsir-engine/         # 引擎节点服务（副节点）
├── WxFbsir-framework/      # 框架核心模块
├── WxFbsir-generator/      # 代码生成模块
├── WxFbsir-quartz/         # 定时任务模块
├── WxFbsir-system/         # 系统模块
├── WxFbsir-ui/             # 前端UI模块
├── sql/                    # 数据库脚本
├── pom.xml                 # 项目根pom文件
└── 部署文档.md             # 部署指南
```

## 二、核心配置文件

### 2.1 主节点配置文件 (`WxFbsir-admin/src/main/resources/application.yml`)

#### 项目基本配置
```yaml
wxfbsir:
  name: 福帮手数据智能化系统              # 项目名称（wxfbsir.name）
  version: 1.0.0                       # 项目版本
  copyrightYear: 2025                  # 版权年份
  profile: ${WXFBSIR_FILE_PATH:D:/WxFbsir/uploadPath}  # 文件上传路径
  addressEnabled: false                # 是否获取IP地址
  captchaType: math                    # 验证码类型（math数字计算, char字符验证）
  domain: ${WXFBSIR_DOMAIN:http://localhost:8080}  # 域名配置
```

#### WebSocket配置
```yaml
wxfbsir:
  websocket:
    enabled: true                      # 是否启用WebSocket服务
    path: /ws/engine                   # WebSocket端点路径（重要，勿随意更改）
    max-connections: 10000             # 最大连接数
    max-message-size: 1048576          # 最大消息大小（字节）
    heartbeat-interval: 30             # 心跳间隔（秒）
    heartbeat-timeout: 10              # 心跳超时时间（秒）
    session-cleanup-interval: 60       # 会话清理间隔（秒）
    message-queue-size: 10000          # 消息队列最大大小
```

#### 服务器配置
```yaml
server:
  port: 8080                           # 服务器端口
  servlet:
    context-path: /                    # 应用访问路径
  tomcat:
    uri-encoding: UTF-8                # URI编码
    accept-count: 1000                 # 连接数满后的排队数
    threads:
      max: 800                         # 最大线程数
      min-spare: 100                   # 初始化线程数
```

#### 数据库配置
```yaml
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wxfbsir?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: 
```

#### Redis配置
```yaml
spring:
  data:
    redis:
      host: ${WXFBSIR_REDIS_HOST:127.0.0.1}  # Redis地址
      port: ${WXFBSIR_REDIS_PORT:6379}       # Redis端口
      database: ${WXFBSIR_REDIS_DATABASE:0}  # 数据库索引
      password: ${WXFBSIR_REDIS_PASSWORD:}   # 密码
      timeout: 5s                              # 连接超时时间
      lettuce:
        pool:
          min-idle: 2                        # 最小空闲连接
          max-idle: 10                       # 最大空闲连接
          max-active: 20                     # 最大连接数
          max-wait: 5000ms                   # 最大等待时间
```

#### SpringDoc API文档配置
```yaml
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    tags-sorter: alpha
  group-configs:
    - group: 'default'
      display-name: '测试模块'
      paths-to-match: '/**'
      packages-to-scan: com.wx.fbsir.web.controller.tool
```

### 2.2 前端配置文件 (`WxFbsir-ui/vite.config.js`)

#### 开发服务器配置
```javascript
server: {
  port: 80,                             // 前端访问端口
  host: true,                           // 允许外部访问
  open: true,                           // 自动打开浏览器
  proxy: {
    '/dev-api': {
      target: baseUrl,                  // 后端API代理地址
      changeOrigin: true,
      rewrite: (p) => p.replace(/^\/dev-api/, '')
    },
    '^/v3/api-docs/(.*)': {            // API文档代理
      target: baseUrl,
      changeOrigin: true,
    }
  }
}
```

#### 环境变量配置
```javascript
envDir: './env'                        // 环境变量文件目录
```

### 2.3 引擎节点配置文件 (`WxFbsir-engine/src/main/resources/application.yml`)

#### 服务器配置
```yaml
server:
  port: 8081                           # 引擎节点端口（与Admin不同）
  servlet:
    context-path: /
```

#### Engine节点配置
```yaml
wxfbsir:
  engine:
    ws-url: wss://wx.fbsir.com/fbsir/ws/engine  # 主节点WebSocket地址
    host-id: U3W071801                          # 主机ID（必须向管理员申请）
    version: @engine.version@                   # 节点版本
    connection:
      timeout: 10000                            # 连接超时时间（毫秒）
      heartbeat-interval: 30                    # 心跳间隔（秒）
      heartbeat-timeout: 10                     # 心跳超时时间（秒）
    reconnect:
      enabled: true                             # 是否启用自动重连
      max-retries: 10                           # 最大重试次数
      initial-delay: 30                         # 初始重连延迟（秒）
      max-delay: 30                             # 最大重连延迟（秒）
```

#### Playwright配置
```yaml
wxfbsir:
  engine:
    playwright:
      enabled: true                             # 是否启用Playwright
      data-dir: /path/to/browser/data         # 浏览器数据目录
      headless: false                         # 是否无头模式
      pool:
        max-size: 10                          # 浏览器池最大大小
        min-idle: 2                           # 浏览器池最小空闲数
        session-timeout: 3600000              # 会话超时时间（毫秒）
      browser:
        launch-timeout: 60000                 # 启动超时时间（毫秒）
        navigation-timeout: 30000             # 导航超时时间（毫秒）
        viewport-width: 1280                  # 视口宽度
        viewport-height: 720                  # 视口高度
```

### 2.4 项目根配置 (`pom.xml`)

#### 项目基本信息
```xml
<groupId>com.wx.fbsir</groupId>
<artifactId>WxFbsir</artifactId>
<version>1.0.0</version>
<packaging>pom</packaging>
```

#### 依赖管理
```xml
<properties>
    <wxfbsir.version>1.0.0</wxfbsir.version>
    <spring-boot.version>3.2.0</spring-boot.version>
    <spring-framework.version>6.1.0</spring-framework.version>
    <mysql.version>8.0.33</mysql.version>
    <druid.version>1.2.16</druid.version>
    <mybatis-spring.version>3.0.2</mybatis-spring.version>
    <mybatis.version>3.5.13</mybatis.version>
    <pagehelper.version>1.4.7</pagehelper.version>
    <swagger.version>3.0.0</swagger.version>
    <springdoc.version>2.8.9</springdoc.version>
</properties>
```

## 三、环境变量配置

### 3.1 系统环境变量
- `JAVA_HOME`: Java安装路径
- `MAVEN_HOME`: Maven安装路径
- `NODE_HOME`: Node.js安装路径

### 3.2 项目环境变量
- `WXFBSIR_REDIS_HOST`: Redis服务器地址
- `WXFBSIR_REDIS_PORT`: Redis服务器端口
- `WXFBSIR_REDIS_DATABASE`: Redis数据库索引
- `WXFBSIR_REDIS_PASSWORD`: Redis密码
- `WXFBSIR_FILE_PATH`: 文件上传路径
- `WXFBSIR_DOMAIN`: 项目域名
- `WXFBSIR_TOKEN_SECRET`: 令牌密钥
- `WXFBSIR_AES_SECRET_KEY`: AES加密密钥
- `GITEE_OAUTH_CLIENT_ID`: Gitee OAuth客户端ID
- `GITEE_OAUTH_CLIENT_SECRET`: Gitee OAuth客户端密钥

## 四、数据库配置

### 4.1 数据库脚本
- `sql/wxfbsir.sql`: 主数据库脚本
- `sql/quartz.sql`: 定时任务数据库脚本

### 4.2 数据库配置要点
- 数据库名：`wxfbsir`
- 字符集：`utf8mb4`
- 排序规则：`utf8mb4_unicode_ci`
- 引擎：`InnoDB`

## 五、安全配置

### 5.1 令牌配置
```yaml
token:
  header: Authorization                  # 令牌请求头
  secret: ${WXFBSIR_TOKEN_SECRET:abcdefghijklmnopqrstuvwxyz}  # 令牌密钥
  expireTime: 30                        # 令牌有效期（分钟）
```

### 5.2 XSS防护配置
```yaml
xss:
  enabled: true                         # 是否启用XSS防护
  excludes: /system/notice              # 排除路径
  urlPatterns: /system/*,/monitor/*,/tool/*  # 防护路径
```

### 5.3 防盗链配置
```yaml
referer:
  enabled: false                        # 是否启用防盗链
  allowed-domains: localhost,127.0.0.1,wx.fbsir.com,www.wx.fbsir.com  # 允许的域名
```

## 六、缓存配置

### 6.1 Redis缓存配置
- 默认使用Redis作为缓存
- 支持集群模式
- 缓存过期策略配置

### 6.2 本地缓存配置
- 使用Spring Cache抽象
- 支持多种缓存实现

## 七、日志配置

### 7.1 日志级别配置
```yaml
logging:
  level:
    root: info                         # 根日志级别
    com.wx.fbsir: info                 # 项目日志级别
    org.springframework: warn          # Spring日志级别
```

### 7.2 日志文件配置
- 日志文件位置：`logs/`
- 日志文件名：`wxfbsir.log`
- 日志滚动策略：按天滚动

## 八、定时任务配置

### 8.1 Quartz配置
- 默认情况下定时任务配置被注释掉（适用于单机部署）
- 如需集群部署，取消注释`ScheduleConfig.java`中的配置

### 8.2 自定义定时任务
- 位于`WxFbsir-engine/src/main/java/com/wx/fbsir/engine/playwright/scheduler/`
- 包含定期清理过期截图文件、僵尸进程等任务

## 九、文件上传配置

### 9.1 上传限制
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 20MB              # 单个文件大小限制
      max-request-size: 20MB           # 总上传文件大小限制
```

### 9.2 存储路径
- 默认存储路径：`D:/WxFbsir/uploadPath/`
- 可通过`wxfbsir.profile`配置项修改

## 十、部署配置

### 10.1 Admin服务部署
- 端口：8080
- 上下文路径：/
- 需要数据库和Redis支持
- 配置正确的WebSocket连接地址

### 10.2 Engine服务部署
- 端口：8081（或其他可用端口）
- 需要配置正确的Admin服务WebSocket地址
- 需要主机ID（向管理员申请）
- 配置Playwright相关参数

### 10.3 前端部署
- 端口：80
- 需要配置正确的后端API代理地址
- 构建命令：`npm run build`

## 十一、性能优化配置

### 11.1 连接池配置
- 数据库连接池：Druid
- Redis连接池：Lettuce
- 线程池：Tomcat默认配置已优化

### 11.2 缓存优化
- 多级缓存策略
- 合理的缓存过期时间
- 缓存预热机制

## 十二、监控配置

### 12.1 健康检查
- Actuator端点配置
- 健康状态监控

### 12.2 指标监控
- 内存使用情况
- CPU使用情况
- 线程状态

## 十三、开发环境配置

### 13.1 IDE配置
- 推荐使用IntelliJ IDEA
- JDK版本：17+
- Maven版本：3.8+

### 13.2 开发工具
- Node.js版本：18+
- npm版本：9+ 或 pnpm 8+
- MySQL版本：8.0+

## 十四、配置注意事项

1. **安全配置**：
   - 生产环境务必使用环境变量配置敏感信息
   - 定期更换令牌密钥
   - 配置HTTPS

2. **性能配置**：
   - 根据服务器性能调整线程池大小
   - 合理配置缓存大小
   - 优化数据库连接池参数

3. **部署配置**：
   - Admin和Engine服务端口不能冲突
   - WebSocket连接地址配置正确
   - 主机ID唯一且已注册

4. **环境变量**：
   - 敏感信息通过环境变量配置
   - 避免硬编码配置信息
   - 配置默认值以保证开发便利性

**最后更新**: 2026-01-30