# 福帮手项目完整服务器部署文档

## 项目概述

福帮手（WxFbsir）是一个面向企业团队与内容运营场景的AI工具链与智能协同平台，采用前后端分离架构，包含主节点（Admin）和副节点（Engine）两部分。

- **WxFbsir-admin**: 主节点，负责业务逻辑、数据管理、用户权限等
- **WxFbsir-ui**: 前端项目，基于Vue 3 + Element Plus构建
- **WxFbsir-engine**: 副节点，负责浏览器自动化、AI任务执行等

---

## 📋 目录
- [项目概述](#项目概述)
- [部署环境准备](#部署环境准备)
  - [服务器要求](#服务器要求)
  - [软件依赖](#软件依赖)
- [宝塔面板安装](#宝塔面板安装)
  - [1. 安装宝塔面板](#1-安装宝塔面板)
  - [2. 访问宝塔面板](#2-访问宝塔面板)
  - [3. 安装运行环境](#3-安装运行环境)
- [数据库配置](#数据库配置)
  - [1. 创建数据库](#1-创建数据库)
  - [2. 开启远程连接 (可选)](#2-开启远程连接 (可选))
  - [3. 导入数据库脚本](#3-导入数据库脚本)
- [后端项目部署 (WxFbsir-admin)](#后端项目部署-wxfbsir-admin)
  - [1. 项目打包](#1-项目打包)
  - [2. 上传JAR包到服务器](#2-上传jar包到服务器)
  - [3. 配置环境变量](#3-配置环境变量)
  - [4. 创建系统服务](#4-创建系统服务)
  - [5. 启动服务](#5-启动服务)
- [Engine副节点部署 (WxFbsir-engine)](#engine副节点部署-wxfbsir-engine)
  - [1. 项目打包](#1-项目打包-1)
  - [2. 配置Engine](#2-配置engine)
  - [3. 上传JAR包到服务器](#3-上传jar包到服务器)
  - [4. 安装Playwright浏览器](#4-安装playwright浏览器)
  - [5. 创建Engine系统服务](#5-创建engine系统服务)
  - [6. 启动Engine服务](#6-启动engine服务)
- [前端项目部署 (WxFbsir-ui)](#前端项目部署-wxfbsir-ui)
  - [1. 配置生产环境变量](#1-配置生产环境变量)
  - [2. 项目打包](#2-项目打包)
  - [3. 上传构建文件到服务器](#3-上传构建文件到服务器)
  - [4. 配置Nginx](#4-配置nginx)
  - [5. 重启Nginx](#5-重启nginx)
- [域名和SSL配置](#域名和ssl配置)
  - [1. 域名解析](#1-域名解析)
  - [2. SSL证书配置](#2-ssl证书配置)
- [防火墙配置](#防火墙配置)
  - [1. 开放必要端口](#1-开放必要端口)
  - [2. CentOS/RHEL防火墙配置](#2-centosrhel防火墙配置)
  - [3. Ubuntu/Debian防火墙配置](#3-ubuntudebian防火墙配置)
- [系统优化配置](#系统优化配置)
  - [1. JVM优化参数](#1-jvm优化参数)
  - [2. 系统文件描述符限制](#2-系统文件描述符限制)
  - [3. 系统内核参数优化](#3-系统内核参数优化)
- [监控和日志](#监控和日志)
  - [1. 服务状态监控](#1-服务状态监控)
  - [2. 系统资源监控](#2-系统资源监控)
  - [3. 日志轮转配置](#3-日志轮转配置)
- [备份策略](#备份策略)
  - [1. 数据库备份](#1-数据库备份)
  - [2. 代码和配置备份](#2-代码和配置备份)
- [故障排查](#故障排查)
  - [1. Admin服务启动失败](#1-admin服务启动失败)
  - [2. Engine连接Admin失败](#2-engine连接admin失败)
  - [3. 前端访问异常](#3-前端访问异常)
  - [4. Playwright浏览器启动失败](#4-playwright浏览器启动失败)
- [更新维护](#更新维护)
  - [1. 版本更新流程](#1-版本更新流程)
  - [2. 数据库更新](#2-数据库更新)
- [安全配置](#安全配置)
  - [1. 访问控制](#1-访问控制)
  - [2. 服务安全](#2-服务安全)

---
# 福帮手项目完整服务器部署文档

## 项目概述

福帮手（WxFbsir）是一个面向企业团队与内容运营场景的AI工具链与智能协同平台，采用前后端分离架构，包含主节点（Admin）和副节点（Engine）两部分。

- **WxFbsir-admin**: 主节点，负责业务逻辑、数据管理、用户权限等
- **WxFbsir-ui**: 前端项目，基于Vue 3 + Element Plus构建
- **WxFbsir-engine**: 副节点，负责浏览器自动化、AI任务执行等

---

## 部署环境准备

### 服务器要求
- 操作系统：Linux (Ubuntu 20.04+/CentOS 7+)
- 内存：至少8GB（推荐16GB）
- 硬盘：至少20GB可用空间
- CPU：4核及以上

### 软件依赖
- JDK 17+ (推荐OpenJDK 17)
- Maven 3.8+
- Node.js 18+ (推荐LTS)
- npm 9+ 或 pnpm 8+
- MySQL 8.0+
- Redis 6.0+ (可选)
- 宝塔面板 7.8+

---

## 宝塔面板安装

### 1. 安装宝塔面板
```
# CentOS/RedHat
yum install -y wget && wget -O install.sh http://download.bt.cn/install/install_6.0.sh && sh install.sh ed84842

# Ubuntu/Deepin
wget -O install.sh http://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh ed84842

# Debian
wget -O install.sh http://download.bt.cn/install/install-ubuntu_6.0.sh && bash install.sh ed84842
```

### 2. 访问宝塔面板
- 访问地址：`http://[服务器IP]:8888`
- 根据提示设置用户名和密码

### 3. 安装运行环境
在宝塔面板中依次安装：
- Nginx 1.20+
- MySQL 8.0
- Redis 6.0+
- JDK 17 (通过软件商店安装或手动安装)
- Node.js 18+ (通过软件商店安装或手动安装)
- 等等需要用到的软件
---

## 数据库配置

### 1. 创建数据库
在宝塔面板的MySQL管理中执行：
```
CREATE DATABASE IF NOT EXISTS wxfbsir DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
或者在可视化界面创建数据库和用户
```

### 2. 开启远程连接 (可选)
如果需要从本地工具（如 Navicat）远程连接服务器数据库：

#### 2.1 授予用户远程权限 (推荐 phpMyAdmin 操作)
1. 登录宝塔面板，进入 **数据库** 界面，点击 **phpMyAdmin**。
2. 在 phpMyAdmin 顶部的 **账户 (User accounts)** 标签页中找到 `root`。
3. 点击 **修改权限 (Edit privileges)** -> **登录信息 (Login Information)**。
4. 将 **主机名 (Host name)** 修改为 `%`（代表允许任意主机连接）。
5. 点击 **执行 (Go)** 并重启 MySQL 服务。

#### 2.2 配置文件检查
确保 MySQL 配置文件（通常位于 `/etc/my.cnf` 或宝塔管理界面的配置修改）中：
- 确认没有 `bind-address = 127.0.0.1`，如有请将其修改为 `bind-address = 0.0.0.0` 或直接注释掉。

#### 2.3 安全策略放行
1. **宝塔面板**: 在 **安全** 菜单中放行 **3306** 端口。
2. **云服务器**: 在阿里云/腾讯云控制台的 **安全组** 中放行 **TCP 3306** 端口。


### 3. 导入数据库脚本
将项目中的SQL文件导入数据库：
- `sql/wxfbsir.sql` - 主数据库脚本
- `sql/update_*.sql` - 更新脚本（如有）

---

## 后端项目部署 (WxFbsir-admin)

### 1. 项目打包
在本地或CI/CD环境中执行（在项目根目录下）：

```bash
# 确保在项目根目录下执行，-pl 指定打包模块，-am 同时打包依赖模块
mvn clean package -DskipTests -pl WxFbsir-admin -am
```

生成的JAR包位置：`WxFbsir-admin/target/wxfbsir-admin-[version].jar`

### 2. 上传JAR包到服务器
将打包好的JAR包上传到服务器指定目录，例如：
```
/home/www/wxfbsir/admin/wxfbsir-admin.jar
```

### 3. 配置环境变量
创建环境配置文件 `/home/www/wxfbsir/admin/fbsir.env`：
```
# 数据库配置
WXFBSIR_MYSQL_URL=jdbc:mysql://localhost:3306/wxfbsir?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&allowMultiQueries=true
WXFBSIR_MYSQL_USERNAME=your_username
WXFBSIR_MYSQL_PASSWORD=your_password

# Redis配置（如果使用）
WXFBSIR_REDIS_HOST=localhost
WXFBSIR_REDIS_PORT=6379
WXFBSIR_REDIS_PASSWORD=your_redis_password
WXFBSIR_AES_SECRET_KEY=your_key
WXFBSIR_REDIS_DATABASE=0	

# 文件上传路径
WXFBSIR_FILE_PATH=/home/www/wxfbsir/upload

# value值随便配置仅如此程序也可以运行
WXFBSIR_TOKEN_SECRET=your_token
WXFBSIR_DRUID_USERNAME=your_druid_username
WXFBSIR_DRUID_PASSWORD=your_druid_pass

# Gitee OAuth配置（如果使用）
GITEE_OAUTH_CLIENT_ID=your_gitee_client_id
GITEE_OAUTH_CLIENT_SECRET=your_gitee_client_secret
GITEE_OAUTH_CALLBACK_URL=http://your_domain/auth
GITEE_OAUTH_FRONTEND_URL=http://your_domain

# 域名配置
WXFBSIR_DOMAIN=http://your_domain

#其他需要的配置
#自行添加
```

### 4. 创建系统服务
创建系统服务文件 `/etc/systemd/system/wxfbsir-admin.service`：

```
[Unit]
Description=WxFbsir Admin Service
After=network.target

[Service]
Type=simple
User=www
Group=www
WorkingDirectory=/home/www/wxfbsir/admin
# ExecStart /usr/bin/java替换成自己的 JDK 路径
ExecStart=/usr/bin/java -jar -Xms512m -Xmx2048m /home/www/wxfbsir/admin/wxfbsir-admin.jar
EnvironmentFile=/home/www/wxfbsir/admin/fbsir.env
SuccessExitStatus=143
TimeoutStopSec=10
Restart=on-failure
RestartSec=30

[Install]
WantedBy=multi-user.target
```

### 5. 启动服务
```
# 重新加载systemd配置
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start wxfbsir-admin

# 设置开机自启
sudo systemctl enable wxfbsir-admin

# 查看服务状态
sudo systemctl status wxfbsir-admin

# 查看服务日志
sudo journalctl -u wxfbsir-admin -f
```

---

## Engine副节点部署 (WxFbsir-engine)

### 1. 项目打包
在本地或CI/CD环境中执行：

```
cd WxFbsir-engine
mvn clean package -DskipTests
```

生成的JAR包位置：`target/wxfbsir-engine-[version].jar`


### 2. 配置Engine
创建Engine配置文件 `/home/www/wxfbsir/engine/application.yml`：
```
server:
  port: 8081

wxfbsir:
  engine:
    # 主节点WebSocket连接地址（请替换为实际的主节点地址）
    ws-url: ws://your_admin_server_ip:8080/ws/engine
    # 主机ID（必须在Admin数据库的白名单中）
    host-id: engine-001
    # 节点版本
    version: "1.0.0"
    
    # 连接配置
    connection:
      # 连接超时时间（毫秒）
      timeout: 30000
      # 心跳间隔（毫秒）
      heartbeat-interval: 30000
      # 心跳超时时间（毫秒）
      heartbeat-timeout: 90000
    
    # 重连配置
    reconnect:
      # 是否启用自动重连
      enabled: true
      # 最大重试次数
      max-retries: 10
      # 初始重连延迟（秒）
      initial-delay: 30
      # 最大重连延迟（秒）
      max-delay: 30
      # 退避乘数（1.0 表示固定间隔）
      backoff-multiplier: 1.0
    
    # Playwright浏览器自动化配置
    playwright:
      # 是否启用Playwright
      enabled: true
      # 浏览器数据目录
      data-dir: /home/www/wxfbsir/engine/playwright-data
      # 是否无头模式运行（生产环境建议true）
      headless: true
      
      # 浏览器实例池配置
      instance-pool:
        # 实例池大小，设置为0时自动根据CPU核心数计算
        size: 0
      
      # 浏览器池配置
      pool:
        # 最大并发浏览器实例数，设置为0时启用动态计算
        max-size: 0
        # 最小空件实例数（预热）
        min-idle: 0
        # 会话超时时间（毫秒，默认1小时）
        session-timeout: 3600000
        # 过期会话清理检查间隔（毫秒，默认5分钟）
        cleanup-interval: 300000
        # 获取会话等待超时（毫秒，默认30秒）
        acquire-timeout: 30000
      
      # 浏览器启动配置
      browser:
        # 浏览器启动超时（毫秒，默认60秒）
        launch-timeout: 60000
        # 页面导航超时（毫秒，默认30秒）
        navigation-timeout: 30000
        # 视口宽度（像素）
        viewport-width: 1280
        # 视口高度（像素）
        viewport-height: 720
        # 是否禁用图片加载（减少资源消耗，低性能系统自动启用）
        disable-images: false
        # 是否禁用GPU加速（建议保持true以提高稳定性）
        disable-gpu: true
        # 启动失败最大重试次数
        max-retries: 3
        # 重试间隔基础时间（毫秒，实际等待=重试次数*此值）
        retry-interval: 2000
      
      # 线程池配置
      thread-pool:
        # 核心线程数（设置为0时根据CPU核心数自动计算）
        core-size: 0
        # 最大线程数（设置为0时根据CPU核心数自动计算）
        max-size: 0
        # 线程空闲时间（秒）
        keep-alive-seconds: 60
        # 任务队列大小（设置为0时根据浏览器池大小自动计算）
        queue-capacity: 0

# 日志配置
logging:
  level:
    root: INFO
    com.wx.fbsir.engine: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] - %msg%n"
  file:
    name: /home/www/wxfbsir/engine/logs/engine.log
  logback:
    rollingpolicy:
      max-file-size: 50MB
      max-history: 30

# Actuator健康检查配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true
```


### 3. 上传JAR包到服务器
将打包好的JAR包上传到服务器指定目录，例如：
```
/home/www/wxfbsir/engine/wxfbsir-engine.jar
```

### 4. 安装Playwright浏览器
```
# 安装Playwright依赖
cd /home/www/wxfbsir/engine
java -jar wxfbsir-engine.jar --version

# 或者直接运行安装命令
# 仅适用于Debian/Ubuntu 系列操作系统
# 其他 Linux：先手动装系统依赖 → 再 npx playwright install chromium
npx playwright install --with-deps chromium
```

### 5. 创建Engine系统服务
创建系统服务文件 `/etc/systemd/system/wxfbsir-engine.service`：
```
[Unit]
Description=WxFbsir Engine Service
After=network.target

[Service]
Type=simple
User=www
Group=www
WorkingDirectory=/home/www/wxfbsir/engine
# ExecStart /www/server/java/jdk-17.0.8/bin/java替换成自己的 JDK 路径
ExecStart=/usr/bin/xvfb-run -a --server-args="-screen 0 1280x1024x24" /www/server/java/jdk-17.0.8/bin/java -jar -Xms512m -Xmx2048m /home/www/wxfbsir/engine/wxfbsir-engine-[engine.version].jar --spring.config.location=file:/home/www/wxfbsir/engine/application.yml
SuccessExitStatus=143
TimeoutStopSec=10
Restart=on-failure
RestartSec=30

[Install]
WantedBy=multi-user.target
```

### 6. 启动Engine服务
```
# 重新加载systemd配置
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start wxfbsir-engine

# 设置开机自启
sudo systemctl enable wxfbsir-engine

# 查看服务状态
sudo systemctl status wxfbsir-engine

# 查看服务日志
sudo journalctl -u wxfbsir-engine -f
```

---

## 前端项目部署 (WxFbsir-ui)

### 1. 配置生产环境变量
在 `WxFbsir-ui/.env.production` 文件中配置：
```
# 生产环境API基础路径
VITE_APP_BASE_API = 'https://your_domain/fbsir'
VITE_APP_UPLOAD_API = 'https://your_domain/fbsir'
```

### 2. 项目打包
在本地或CI/CD环境中执行：

```
cd WxFbsir-ui
npm install
npm run build:prod
```

生成的构建文件在 `dist/` 目录中。

### 3. 上传构建文件到服务器
将 `dist` 目录中的所有文件上传到Nginx的网站目录，例如：
```
/home/www/wxfbsir/front/dist/*
```

### 4. 配置Nginx
在宝塔面板中创建网站，或直接编辑Nginx配置文件。注意：配置宝塔面板中的前端配置会覆盖自己直接编辑的Nginx配置，建议直接使用宝塔面板前端配置

直接编辑Nginx配置文件：

创建 `/www/server/panel/vhost/nginx/wxfbsir.conf`：
```
server {
    listen 80;
    listen 443 ssl http2;
    server_name your_domain.com;
    
    # SSL证书配置（如果使用HTTPS）
    ssl_certificate /path/to/your/certificate.crt;
    ssl_certificate_key /path/to/your/private.key;
    
    # 前端静态文件
    location / {
        alias /home/www/wxfbsir/front/dist/;
        try_files $uri $uri/ /index.html;
        expires 1h;
        add_header Cache-Control "public, immutable";
    }
    
    # API代理到后端
    location /fbsir/ {
        proxy_pass http://127.0.0.1:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    # 上传文件代理
    location /profile/ {
        proxy_pass http://127.0.0.1:8080/profile/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1k;
    gzip_comp_level 6;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;
    gzip_disable "MSIE [1-6]\.";
    
    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
}
```

宝塔面板网站THML项目设置中的前端配置文件：

    server {
        listen 80;
        listen 443 ssl ;
        listen [::]:443 ssl ;
        http2 on;
        listen [::]:80;
        server_name your_domain;
    
        # SSL 证书（保留原有）
        ssl_certificate    /www/server/panel/vhost/cert/your_domain/fullchain.pem;
        ssl_certificate_key    /www/server/panel/vhost/cert/your_domain/privkey.pem;
        ssl_protocols TLSv1.1 TLSv1.2 TLSv1.3;
        ssl_ciphers EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;
        ssl_prefer_server_ciphers on;
        ssl_session_tickets on;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;
        add_header Strict-Transport-Security "max-age=31536000";
        add_header Alt-Svc 'quic=":443"; h3=":443"; h3-29=":443"; h3-27=":443";h3-25=":443"; h3-T050=":443"; h3-Q050=":443";h3-Q049=":443";h3-Q048=":443"; h3-Q046=":443"; h3-Q043=":443"';
        error_page 497  https://$host$request_uri;
    
        # 强制 HTTPS 重定向（原有，保留）
        set $isRedcert 1;
        if ($server_port != 443) {
            set $isRedcert 2;
        }
        if ( $uri ~ /\.well-known/ ) {
            set $isRedcert 1;
        }
        if ($isRedcert != 1) {
            rewrite ^(/.*)$ https://$host$1 permanent;
        }
    
        # 前端静态文件根目录（请根据实际位置修改）
        root /home/www/wxfbsir/front/dist;
    
        index index.html;
    
        # 关键：处理 Vue 单页应用路由
        location / {
            try_files $uri $uri/ /index.html;
            expires 1h;
            add_header Cache-Control "public, immutable";
        }
    
        # API 代理到 Admin 后端（端口 8080）
        location /fbsir/ {
            proxy_pass http://127.0.0.1:8080/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
            proxy_read_timeout 90s;
        }
    
        # 上传文件代理（对应后端的 /profile/ 路径）
        location /profile/ {
            proxy_pass http://127.0.0.1:8080/profile/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        # WebSocket 代理（请将 8081 替换为 engine 服务的实际端口）
    location /ws/ {
        proxy_pass http://127.0.0.1:8080;   # 注意：末尾没有斜杠，保留完整路径 /ws/...
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 3600s;
    }
    
        # 可选：代理所有 /api 开头的请求到后端（如果前端使用 /api 前缀）
        location /api/ {
            proxy_pass http://127.0.0.1:8080/api/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /prod-api/ {
        proxy_pass http://127.0.0.1:8080/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 90s;
    }
    
        # 静态资源缓存（保留原有）
        # location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$ {
        #     expires 30d;
        #     error_log /dev/null;
        #     access_log /dev/null;
        # }
        location ~ ^/(?!profile/).*\.(gif|jpg|jpeg|png|bmp|swf)$ {
          expires 30d;
          error_log /dev/null;
          access_log /dev/null;
        }
        location ~ .*\.(js|css)?$ {
            expires 12h;
            error_log /dev/null;
            access_log /dev/null;
        }
    
        # 禁止访问敏感文件（保留原有）
        location ~ ^/(\.user.ini|\.htaccess|\.git|\.env|\.svn|\.project|LICENSE|README.md) {
            return 404;
        }
    
        # SSL 验证目录（保留原有）
        location ~ \.well-known {
            allow all;
        }
    
        access_log  /www/wwwlogs/xhh.fbsir.com.log;
        error_log  /www/wwwlogs/xhh.fbsir.com.error.log;
    }
### 5. 重启Nginx

在宝塔面板中重启Nginx，或执行：
```
sudo nginx -s reload
```

---

## 域名和SSL配置

### 1. 域名解析
将域名解析到服务器IP地址：
- `@` 记录指向服务器IP
- `www` 记录指向服务器IP

### 2. SSL证书配置
在宝塔面板中为网站申请SSL证书：
- 选择"Let's Encrypt"免费证书
- 或上传自己的证书文件

---

## 防火墙配置

### 1. 开放必要端口
在服务器安全组和系统防火墙中开放以下端口：
- `80` (HTTP)
- `443` (HTTPS)
- `8080` (Admin后端，如不通过Nginx代理则需要)
- `9090` (Engine服务，如需要外部访问)

### 2. CentOS/RHEL防火墙配置
```
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --permanent --add-port=9090/tcp
sudo firewall-cmd --reload
```

### 3. Ubuntu/Debian防火墙配置
```
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 8080/tcp
sudo ufw allow 9090/tcp
sudo ufw reload
```

---

## 系统优化配置

### 1. JVM优化参数
根据服务器资源配置合适的JVM参数：
```
# Admin服务JVM参数
# jar包名字改成自己的
java -Xms1024m -Xmx3072m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UseStringDeduplication -jar WxFbsir-admin.jar

# Engine服务JVM参数（如果内存充足）
# jar包名字改成自己的
java -Xms1024m -Xmx3072m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -jar wxfbsir-engine-[engine.version].jar
```

### 2. 系统文件描述符限制
编辑 `/etc/security/limits.conf`：
```
www soft nofile 65535
www hard nofile 65535
```

### 3. 系统内核参数优化
编辑 `/etc/sysctl.conf`：
```
# TCP优化
net.core.somaxconn = 65535
net.ipv4.tcp_max_syn_backlog = 65535
net.ipv4.tcp_fin_timeout = 30
net.ipv4.tcp_keepalive_time = 1200
net.ipv4.ip_local_port_range = 1024 65535
```

应用配置：
```bash
sudo sysctl -p
```

---

## 监控和日志

### 1. 服务状态监控
```
# 查看所有服务状态
sudo systemctl status wxfbsir-admin wxfbsir-engine

# 查看实时日志
sudo journalctl -u wxfbsir-admin -f
sudo journalctl -u wxfbsir-engine -f
```

### 2. 系统资源监控
```
# 查看系统资源使用情况
htop
# 或
top

# 查看磁盘使用情况
df -h

# 查看内存使用情况
free -h
```

### 3. 日志轮转配置
创建日志轮转配置 `/etc/logrotate.d/wxfbsir`：
```
/home/www/wxfbsir/admin/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}

/home/www/wxfbsir/engine/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
```

---

## 备份策略

### 1. 数据库备份
创建定时任务备份数据库：
```bash
# 编辑定时任务
crontab -e

# 添加每日凌晨2点备份数据库
0 2 * * * /usr/bin/mysqldump -u root -pyour_password wxfbsir > /home/backup/db_backup_$(date +\%Y\%m\%d).sql
```

### 2. 代码和配置备份
定期备份配置文件和代码：
```bash
# 创建备份脚本
cat > /home/scripts/backup.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/home/backup/$DATE"
mkdir -p $BACKUP_DIR

# 备份配置文件
cp -r /home/www/wxfbsir/admin/fbsir.env $BACKUP_DIR/
cp -r /home/www/wxfbsir/engine/application.yml $BACKUP_DIR/

# 备份数据库
mysqldump -u root -pyour_password wxfbsir > $BACKUP_DIR/db_backup.sql

# 打包备份
tar -czf /home/backup/wxfbsir_backup_$DATE.tar.gz -C /home/backup $DATE
rm -rf /home/backup/$DATE
EOF

chmod +x /home/scripts/backup.sh
```

---

## 故障排查

### 1. Admin服务启动失败

- 检查数据库连接是否正常
- 检查端口是否被占用
- 查看日志文件中的错误信息

### 2. Engine连接Admin失败
- 检查网络连通性
- 检查WebSocket地址配置
- 检查主机ID是否在白名单中
- 检查防火墙设置

### 3. 前端访问异常
- 检查Nginx配置
- 检查API代理设置
- 检查SSL证书配置

### 4. Playwright浏览器启动失败

- 检查系统依赖是否安装完整
- 检查文件权限
- 检查内存是否充足

### 5.WebSocket连接失败

- 检查前端配置是否添加WebSocket支持
- 检查文件权限
- 检查SSL证书配置

### 6.登录管理器显示：登录失败：未知错误

- 检查engine配置中的线程池数量是否过低

- 检查日志开头有 `xvfb-run` 字样，确保 xvfb 正常运行，程序需要图形渲染

- 检查浏览器缓存是否超载


### 7.数据库远程连接失败 :1045 Access denied

  - 检查 phpMyAdmin 账户列表中，对应的用户名（如 `root`）的 Host 是否确认为 `%`。
  - 确认本地连接时填写的 IP 是服务器的公网 IP。
  - 确认密码是否填写正确。

---

## 更新维护

### 1. 版本更新流程
1. 停止服务：`sudo systemctl stop wxfbsir-admin wxfbsir-engine`
2. 备份当前版本
3. 上传新版本文件
4. 更新配置（如有变化）
5. 启动服务：`sudo systemctl start wxfbsir-admin wxfbsir-engine`
6. 验证服务状态

### 2. 数据库更新
- 更新前先备份数据库
- 按照升级文档执行数据库脚本
- 验证数据完整性

---

## 安全配置

### 1. 访问控制
- 配置强密码策略
- 定期更换密码
- 限制SSH访问IP

### 2. 服务安全
- 定期更新系统补丁
- 配置防火墙规则
- 监控异常访问

---

**文档更新日期**: 2026年2月2日
**版本**: V1.0